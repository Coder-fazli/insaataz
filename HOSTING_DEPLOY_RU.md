# Размещение на Хостинге - Простая Инструкция

---

## 📍 Структура Папок на Хостинге

На хостинге обычно есть 2 основные папки:

```
/home/имя_пользователя/          ← Ваша основная папка (скрытая)
/home/имя_пользователя/public_html/   ← Видимая часть сайта
```

Или:
```
/home/имя_пользователя/
/home/имя_пользователя/www/      ← Видимая часть сайта
```

---

## 🎯 2 СПОСОБА РАЗМЕЩЕНИЯ

### ⭐ СПОСОБ 1: Простой и Безопасный (Рекомендуется)

#### Шаг 1: Загрузите весь проект

**В основную папку хостинга** (ВНЕ public_html):

```
/home/имя_пользователя/insaat.az/
├── app/
├── bootstrap/
├── config/
├── database/
├── public/           ← ЭТО ОЧЕНЬ ВАЖНО!
├── resources/
├── routes/
├── storage/
├── vendor/
├── .env
├── artisan
└── composer.json
```

#### Шаг 2: Скопируйте содержимое папки public

В **public_html** скопируйте только **содержимое** папки `public/`:

```
/home/имя_пользователя/public_html/
├── css/
├── js/
├── storage/          ← Картинки здесь
├── webcoder/
├── .htaccess         ← ОЧЕНЬ ВАЖНО!
└── index.php         ← ОЧЕНЬ ВАЖНО!
```

#### Шаг 3: Отредактируйте файл index.php

Откройте `public_html/index.php` и измените эти строки:

**БЫЛО:**
```php
require __DIR__.'/../vendor/autoload.php';
$app = require_once __DIR__.'/../bootstrap/app.php';
```

**СТАЛО (добавьте название папки в начало пути):**
```php
require __DIR__.'/../insaat.az/vendor/autoload.php';
$app = require_once __DIR__.'/../insaat.az/bootstrap/app.php';
```

✅ **ГОТОВО!** Ваш сайт будет работать!

---

### СПОСОБ 2: Всё в public_html (Проще, но менее безопасно)

Если хостинг позволяет изменить document root:

#### Шаг 1: Загрузите весь проект в public_html

```
/home/имя_пользователя/public_html/
├── app/
├── bootstrap/
├── config/
├── public/          ← Это проблематично
├── storage/
├── .env
└── ...
```

#### Шаг 2: Измените Document Root

cPanel → Domains → Document Root:
```
Старый: /home/имя_пользователя/public_html
Новый:  /home/имя_пользователя/public_html/public
```

✅ **ГОТОВО!**

---

## 📦 Что Загружать

### ОБЯЗАТЕЛЬНО загрузите:

✅ **Папки:**
- `app/`
- `bootstrap/`
- `config/`
- `database/`
- `public/` (или её содержимое)
- `resources/`
- `routes/`
- `storage/`
- `vendor/`

✅ **Файлы:**
- `.env` ← **ОЧЕНЬ ВАЖНО!** (данные БД здесь)
- `.htaccess`
- `artisan`
- `composer.json`
- `composer.lock`

### НЕ загружайте (не нужны):

❌ `node_modules/` - Очень большая, не нужна
❌ `.git/` - Не нужна
❌ `.idea/` - Не нужна
❌ `*.zip` файлы
❌ `*.sql` файлы (импортируете отдельно)

---

## 🗄️ База Данных

### Шаг 1: Создайте БД на хостинге

cPanel → MySQL Databases:
- Имя базы данных: напр. `user_insaat`
- Пользователь: напр. `user_admin`
- Пароль: сильный пароль
- Добавьте пользователя к БД (ALL PRIVILEGES)

### Шаг 2: Обновите файл .env

```env
DB_CONNECTION=mysql
DB_HOST=localhost
DB_PORT=3306
DB_DATABASE=user_insaat       ← Новое имя БД
DB_USERNAME=user_admin        ← Новый пользователь
DB_PASSWORD=новый_пароль      ← Новый пароль
```

### Шаг 3: Импортируйте SQL файл

cPanel → phpMyAdmin:
- Выберите базу данных
- Import → выберите файл `hbecxexb_db.sql`
- Go

✅ **ГОТОВО!**

---

## 🔐 Безопасность (Важно!)

### Права доступа к папкам (Permissions):

```bash
chmod 755 storage/
chmod 755 storage/app/
chmod 755 storage/framework/
chmod 755 storage/logs/
chmod 755 bootstrap/cache/
```

Или через cPanel File Manager → Правый клик → Permissions → 755

### Файл .env:

```bash
chmod 644 .env
```

**ВАЖНО:** Файл .env должен быть ВНЕ public_html!

---

## ✅ Проверка

Откройте сайт: `https://ваш-домен.com`

### Если работает:
✅ Главная страница открывается
✅ Картинки отображаются
✅ Слайдер партнёров показывает логотипы
✅ Сертификаты отображаются

### Если есть ошибка:

**"500 Internal Server Error":**
- Файл `.htaccess` загружен правильно?
- Папка `storage/` имеет права 755?

**"Mix file not found":**
- Папки `public/css/` и `public/js/` есть?

**Картинки не отображаются:**
- Папка `public/storage/` есть?
- Файлы в `public/storage/certificates/` есть?
- Файлы в `public/storage/partners/` есть?

**Ошибка БД:**
- Данные БД в файле `.env` правильные?
- БД импортирована?

---

## 📞 Техническая Поддержка

Если проблема продолжается:
1. Обратитесь в службу поддержки хостинга
2. Укажите версию Laravel: Laravel 9.x
3. Требуемая версия PHP: 8.1 или 8.2

---

## 🎯 ВАЖНЫЕ ЗАМЕЧАНИЯ

✅ **Проект ГОТОВ!**
- Все файлы в `public/storage/`
- Никаких дополнительных команд не нужно
- Просто загрузите и всё заработает

✅ **Редактирование index.php:**
- Это единственное изменение
- Изменяются только 2 строки

✅ **Файл .env:**
- Изменяйте только данные БД
- Больше ничего не меняйте

---

## 📋 КРАТКИЙ ЧЕКЛИСТ

### Перед загрузкой:
- [ ] Все файлы проекта готовы
- [ ] SQL файл есть
- [ ] Знаете данные для БД на хостинге

### На хостинге:
- [ ] Создали БД
- [ ] Загрузили весь проект (кроме node_modules, .git)
- [ ] Скопировали содержимое public/ в public_html/
- [ ] Отредактировали index.php (2 строки)
- [ ] Обновили .env файл
- [ ] Импортировали SQL
- [ ] Установили права 755 на storage/
- [ ] Проверили сайт

---

## 💡 ПРИМЕР: Что и Куда

**НА ЛОКАЛЬНОМ КОМПЬЮТЕРЕ:**
```
C:\Users\Victor\Desktop\Freelannce\Zamin\insaat.az\
├── app/
├── public/
│   ├── index.php
│   ├── storage/
│   └── ...
├── storage/
└── ...
```

**НА ХОСТИНГЕ:**
```
/home/username/
│
├── insaat.az/              ← Весь проект сюда
│   ├── app/
│   ├── bootstrap/
│   ├── config/
│   ├── public/
│   ├── resources/
│   ├── routes/
│   ├── storage/
│   ├── vendor/
│   ├── .env              ← Обновить данные БД
│   └── artisan
│
└── public_html/            ← Только содержимое public/ сюда
    ├── css/
    ├── js/
    ├── storage/            ← ВСЕ КАРТИНКИ УЖЕ ЗДЕСЬ!
    │   ├── certificates/
    │   └── partners/
    ├── webcoder/
    ├── .htaccess
    └── index.php          ← Редактировать этот файл
```

---

## 🚀 ГОТОВО К ДЕПЛОЮ!

Проект полностью готов к размещению на хостинге!

Все файлы на своих местах:
- ✅ Сертификаты: 63 файла
- ✅ Партнёры: 11 файлов
- ✅ Продукты: все фото
- ✅ Слайдеры: все фото

**Просто загрузите и отредактируйте index.php!**

---

**Последнее обновление:** 2025-11-21
**Статус:** ✅ Протестировано, работает
